# Import Scripts

This directory contains JSON files used for importing machine data.

## Files

- `machines_import_*.json` - Machine data files for bulk import

## Usage

These files are used by the import page at `/machines/import` to bulk import machine data into the system.

## Note

These are data files, not source code. They should be version controlled but are separate from the application code.
